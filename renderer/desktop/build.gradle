apply plugin: 'java'

repositories {
    mavenCentral()
}

sourceCompatibility = 1.5
targetCompatibility = 1.5

dependencies {
	compile project(':renderer:share')
	testCompile 'junit:junit:4.11'
}

eclipse.project.name = 'renderer-desktop'
